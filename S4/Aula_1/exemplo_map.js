const arrayExemplo = [1,2,3,4,5];

const arrayMap = arrayExemplo.map((item) => {
  return item * 2; 
});

console.log(arrayMap); //irá retornar o valor do arrayMap formado a partir da operação de map no arrayExemplo
