const dados = [123, 'texto', true, 'palavra', 99, 'JS'];

for (const item of dados) {
    if (typeof item === 'string') {
        console.log(item);
    }
}

// for ( const VariavelQueVaiReceberOItem of ListaQueVaiSerLida )
